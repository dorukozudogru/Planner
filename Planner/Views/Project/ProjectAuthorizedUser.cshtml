@model IEnumerable<Planner.DataAccess.vwAuthorizedUserProject>

@{
    ViewBag.Title = "Projeyi Kimler Görebiliyor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm(Html.BeginForm("RemoveAuthorizeToUserForProject", "Project", FormMethod.Post, new { role = "form" })))
{
    <div class="panel">
        <div class="panel-body">
            <div class="example-wrap">
                <h4 class="example-title">Yetkili Kullanıcılar</h4>
                <h6 class="note"><small>(Lütfen silmek istediklerinizi seçiniz)</small></h6>
                <div>
                    <table class="table table-hover" data-plugin="selectable" data-row-selectable="true">
                        <thead>
                            <tr>
                                <th class="w-50">
                                    <span class="checkbox-custom checkbox-primary">
                                        <input class="selectable-all" type="checkbox">
                                    </span>
                                </th>
                                <th>
                                    Adı Soyadı
                                </th>
                                <th>
                                    E-Posta
                                </th>
                                <th>
                                    Admin Mi?
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <span class="checkbox-custom checkbox-success">
                                            <input class="selectable-item" checked type="checkbox" name="users" value="@item.UserId">
                                            <label></label>
                                        </span>
                                    </td>
                                    <td>@item.UserName @item.UserSurname</td>
                                    <td>@item.UserEMail</td>
                                    <td>@item.UserIsAdmin</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div>
                    <input type="submit" class="btn btn-success" value="Seçili Olanları Sil" />
                </div>
            </div>
        </div>
    </div>
}